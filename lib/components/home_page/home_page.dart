import 'package:flutter/material.dart';
import 'package:weight_track_app/components/home_page/exercise_day_overview.dart';


// TODO: don't forget to add proper state management for when something is added to the db (also update the ui)

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ExerciseDayOfSplitPage();
  }
}

